import 'package:flutter/material.dart';

class RequestDeliveryPage extends StatelessWidget {
  const RequestDeliveryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
